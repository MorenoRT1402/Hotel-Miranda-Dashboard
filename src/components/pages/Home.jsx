import styled from "styled-components";

const Title = styled.h1`
    /* color: white; */
`;

export const Home = () => {

    return (
        <Title className="color-secondary">Home</Title>
    );
};
